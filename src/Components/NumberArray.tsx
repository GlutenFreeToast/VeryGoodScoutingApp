// src/types/index.ts or a similar file

// You can use a type alias
export type NumberArrayType = number[];

// Or an interface, which is slightly more verbose for a simple primitive array
export interface INumberArray extends Array<number> {}
